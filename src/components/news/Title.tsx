import { Trans } from "../common"

const Title = () => {
    return (
        <h1 className="uppercase font-semibold text-4xl mb-8">
            <Trans text="news.title" />
        </h1>
    )
}

export default Title
